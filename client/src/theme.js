import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    background: { lightDark: "#222431", dark: "#212430" },
    primary: { main: "#A67CF8" },
    common: { lightGray: "#9699A4" },
  },
});

export default theme;
